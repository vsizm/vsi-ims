"use client";
import Link from "next/link";import{FormEvent,useEffect,useMemo,useState}from"react";
type P={id:string;code:string;name:string};type R={id:string;programmeId:string;code:string;name:string;objective:string;status:string};type B={id:string;level:string;programmeId:string|null;projectId:string|null;financialYear:number;budgetCode:string;amountZmw:string;status:string};
export default function ProjectsPage(){const[ps,setPs]=useState<P[]>([]),[rs,setRs]=useState<R[]>([]),[bs,setBs]=useState<B[]>([]),[open,setOpen]=useState(false),[search,setSearch]=useState(""),[message,setMessage]=useState(""),[busy,setBusy]=useState(false);const[f,setF]=useState({programmeId:"",code:"",name:"",objective:"",financialYear:String(new Date().getFullYear()),budgetCode:"",amountZmw:""});async function api(path:string,o:RequestInit={}){const r=await fetch(path,{...o,headers:{"Content-Type":"application/json",...(o.headers||{})}});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||`Request failed (${r.status})`);return d}async function load(){const[p,r,b]=await Promise.all([api("/api/programmes"),api("/api/projects"),api("/api/finance/budgets")]);setPs(p);setRs(r);setBs(b.filter((x:B)=>x.level==="PROJECT"))}useEffect(()=>{load().catch(e=>setMessage(e instanceof Error?e.message:"Unable to load projects."))},[]);const filter=new URLSearchParams(typeof window==="undefined"?"":window.location.search).get("programme");const visible=useMemo(()=>rs.filter(r=>(!filter||r.programmeId===filter)&&`${r.code} ${r.name}`.toLowerCase().includes(search.toLowerCase())),[rs,search,filter]);async function create(e:FormEvent){e.preventDefault();setBusy(true);setMessage("");try{const r=await api("/api/projects",{method:"POST",body:JSON.stringify(f)});const all=await api("/api/finance/budgets");const parent=all.find((b:B)=>b.level==="PROGRAMME"&&b.programmeId===f.programmeId&&b.financialYear===Number(f.financialYear)&&b.status==="APPROVED");if(!parent)throw new Error("Project created, but no approved Programme budget exists for this financial year. Approve the Programme allocation first.");await api("/api/finance/budgets",{method:"POST",body:JSON.stringify({level:"PROJECT",projectId:r.id,parentBudgetId:parent.id,financialYear:Number(f.financialYear),budgetCode:f.budgetCode,amountZmw:Number(f.amountZmw)})});setOpen(false);setF({...f,code:"",name:"",objective:"",budgetCode:"",amountZmw:""});await load();setMessage("Project and budget allocation created.")}catch(e){setMessage(e instanceof Error?e.message:"Unable to create project and budget.")}finally{setBusy(false)}}return <main className="ims-shell"><aside className="sidebar"><div className="sidebar-brand"><div className="brand-mark">VSI</div><div><strong>VSI IMS</strong><span>Information Management</span></div></div><div className="workspace-label">WORKSPACE</div><nav className="sidebar-nav"><Link className="sidebar-link" href="/dashboard">⌂ <span>Dashboard</span></Link><Link className="sidebar-link" href="/dashboard/directorates">▤ <span>Directorates</span></Link><Link className="sidebar-link" href="/dashboard/programmes">▣ <span>Programmes</span></Link><Link className="sidebar-link active" href="/dashboard/projects">◫ <span>Projects</span></Link><Link className="sidebar-link" href="/dashboard/activities">✓ <span>Activities</span></Link><Link className="sidebar-link" href="/dashboard/finance">₭ <span>Financial Intelligence</span></Link></nav></aside><section className="workspace"><header className="topbar"><div className="breadcrumb"><span>VSI IMS</span><i>/</i><strong>Projects</strong></div></header><div className="page-content"><div className="title-bar"><div><p className="eyebrow">PROJECT MANAGEMENT</p><h1>Projects</h1><p>Projects allocate against an approved Programme budget.</p></div><button className="primary-button" onClick={()=>setOpen(v=>!v)}>{open?"Close":"+ New project"}</button></div>{message&&<div className="flash">{message}</div>}{open&&<section className="create"><h2>New project & budget</h2><form onSubmit={create}><label>Programme<select required value={f.programmeId} onChange={e=>setF({...f,programmeId:e.target.value})}><option value="">Select programme</option>{ps.map(p=><option key={p.id} value={p.id}>{p.code} · {p.name}</option>)}</select></label><label>Project code<input required value={f.code} onChange={e=>setF({...f,code:e.target.value})}/></label><label>Project name<input required value={f.name} onChange={e=>setF({...f,name:e.target.value})}/></label><label>Objective<textarea required value={f.objective} onChange={e=>setF({...f,objective:e.target.value})}/></label><div className="two"><label>Financial year<input type="number" min="2020" max="2100" required value={f.financialYear} onChange={e=>setF({...f,financialYear:e.target.value})}/></label><label>Budget code<input required value={f.budgetCode} onChange={e=>setF({...f,budgetCode:e.target.value})}/></label></div><label>Project allocation (ZMW)<input required type="number" min="0.01" step="0.01" value={f.amountZmw} onChange={e=>setF({...f,amountZmw:e.target.value})}/></label><button className="action" disabled={busy}>{busy?"Saving…":"Create project & allocation"}</button></form></section>}<div className="toolbar"><input className="search" placeholder="Search projects…" value={search} onChange={e=>setSearch(e.target.value)}/></div><section className="list">{visible.map(r=><article className="record" key={r.id}><div><span className="code">{r.code}</span><h2>{r.name}</h2><p>{r.objective}</p></div><span className="pill">{ps.find(p=>p.id===r.programmeId)?.name||"Unlinked programme"}</span><div className="budget"><strong>{bs.filter(b=>b.projectId===r.id).map(b=>`ZMW ${Number(b.amountZmw).toLocaleString()}`).join(" · ")||"No allocation"}</strong><span>{bs.find(b=>b.projectId===r.id)?.status||"—"}</span></div><Link className="action secondary" href={`/dashboard/activities?project=${r.id}`}>Activities →</Link></article>)}</section></div></section><style jsx>{`.flash{margin-top:18px;padding:10px;border-radius:8px;background:#edf8f2;color:#16744d;font-size:.82rem;font-weight:700}.create{margin-top:18px;padding:20px;background:#fff;border:1px solid var(--line);border-radius:12px;max-width:760px}.create form{display:grid;gap:10px}.create label{display:grid;gap:5px;font-size:.76rem;font-weight:800;color:var(--regal-navy)}.create input,.create textarea,.create select{border:1px solid var(--line);border-radius:8px;padding:10px;font:inherit}.create textarea{min-height:80px}.two{display:grid;grid-template-columns:1fr 1fr;gap:10px}.action{border:0;border-radius:8px;padding:10px 14px;background:var(--regal-navy);color:#fff;font-weight:800;text-decoration:none}.secondary{background:#e9f0f5;color:var(--regal-navy)}.toolbar{margin:18px 0}.search{width:100%;max-width:420px;border:1px solid var(--line);border-radius:8px;padding:10px}.list{display:grid;gap:12px}.record{display:grid;grid-template-columns:minmax(0,1fr) 180px 190px auto;gap:14px;align-items:center;background:#fff;border:1px solid var(--line);border-radius:12px;padding:16px}.record h2{margin:4px 0;color:var(--regal-navy);font-size:1rem}.record p{color:var(--muted);font-size:.8rem}.code{font-weight:900;color:var(--baltic-blue)}.pill,.budget span{font-size:.7rem;color:var(--muted)}.budget{display:grid;gap:4px}@media(max-width:900px){.record{grid-template-columns:1fr}.two{grid-template-columns:1fr}}`}</style></main>}

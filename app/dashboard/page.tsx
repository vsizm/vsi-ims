import Link from "next/link";
import { count, desc, eq } from "drizzle-orm";
import { activities, programmes, projects, reports, volunteers, volunteerAssignments } from "@/db/schema";
import { database } from "@/lib/db";

export const dynamic = "force-dynamic";
const BENEFICIARY_TARGET = 100_000;
const navItems = [["Dashboard","⌂","/dashboard"],["Programmes","▣","/dashboard/workflow#programmes"],["Projects","◫","/dashboard/workflow#projects"],["Activities","✓","/dashboard/workflow#activities"],["Reports","▤","/dashboard/workflow#reports"],["Financial Intelligence","₭","/dashboard/finance"],["Volunteer Management","♙","/dashboard/volunteers"]];
function Icon({ children }: { children: React.ReactNode }) { return <span className="nav-icon" aria-hidden="true">{children}</span>; }
function formatDate(value: Date | string | null) { if (!value) return "—"; return new Intl.DateTimeFormat("en-GB", { day: "2-digit", month: "short", year: "numeric" }).format(new Date(value)); }

export default async function Dashboard() {
  const db = database();
  const [programmeCount, activeProgrammeCount, projectCount, activeProjectCount, activityCount, reportCount, recentActivities] = await Promise.all([
    db.select({ value: count() }).from(programmes), db.select({ value: count() }).from(programmes).where(eq(programmes.active, true)), db.select({ value: count() }).from(projects), db.select({ value: count() }).from(projects).where(eq(projects.status, "ACTIVE")), db.select({ value: count() }).from(activities), db.select({ value: count() }).from(reports), db.select({ id: activities.id, title: activities.title, status: activities.status, dueDate: activities.dueDate, programmeCode: programmes.code }).from(activities).leftJoin(projects, eq(activities.projectId, projects.id)).leftJoin(programmes, eq(projects.programmeId, programmes.id)).orderBy(desc(activities.createdAt)).limit(5)
  ]);

  let volunteerCount = [{ value: 0 }];
  let activeVolunteerCount = [{ value: 0 }];
  let assignmentCount = [{ value: 0 }];
  try {
    [volunteerCount, activeVolunteerCount, assignmentCount] = await Promise.all([
      db.select({ value: count() }).from(volunteers),
      db.select({ value: count() }).from(volunteers).where(eq(volunteers.status, "ACTIVE")),
      db.select({ value: count() }).from(volunteerAssignments).where(eq(volunteerAssignments.status, "ACTIVE"))
    ]);
  } catch {
    // The volunteer module is additive. Keep the core dashboard available while its migration is pending.
  }

  const programmesTotal=Number(programmeCount[0]?.value??0), activeProgrammes=Number(activeProgrammeCount[0]?.value??0), projectsTotal=Number(projectCount[0]?.value??0), activeProjects=Number(activeProjectCount[0]?.value??0), activitiesTotal=Number(activityCount[0]?.value??0), reportsTotal=Number(reportCount[0]?.value??0), volunteersTotal=Number(volunteerCount[0]?.value??0), activeVolunteers=Number(activeVolunteerCount[0]?.value??0), activeAssignments=Number(assignmentCount[0]?.value??0);
  const inProgress=recentActivities.filter(i=>i.status==="IN_PROGRESS").length, planned=recentActivities.filter(i=>i.status==="PLANNED").length, activityHealth=activitiesTotal===0?0:Math.min(100,Math.round(((inProgress+planned)/activitiesTotal)*100));
  return <main className="ims-shell"><aside className="sidebar"><div className="sidebar-brand"><div className="brand-mark">VSI</div><div><strong>VSI IMS</strong><span>Information Management</span></div></div><div className="workspace-label">WORKSPACE</div><nav className="sidebar-nav" aria-label="Main navigation">{navItems.map(([label,icon,href],index)=><Link key={label} className={`sidebar-link ${index===0?"active":""}`} href={href}><Icon>{icon}</Icon><span>{label}</span></Link>)}</nav><div className="sidebar-divider"/><div className="workspace-label">SYSTEM</div><div className="sidebar-link muted-link"><Icon>◉</Icon><span>Health</span><b className="online-dot">●</b></div><div className="sidebar-link muted-link"><Icon>⚙</Icon><span>Configuration</span></div><div className="sidebar-bottom"><div className="user-card"><div className="avatar">PA</div><div><strong>Programme Admin</strong><span>VSI</span></div><span className="user-menu">⋮</span></div></div></aside><section className="workspace"><header className="topbar"><div className="breadcrumb"><span>VSI IMS</span><i>/</i><strong>Dashboard</strong></div><div className="top-actions"><span className="env-pill"><span/> Connected</span><button className="icon-button" aria-label="Notifications">♢</button><div className="top-avatar">PA</div></div></header><div className="page-content"><div className="title-bar"><div><p className="eyebrow">VISIONARY STUDENTS INITIATIVE</p><h1>Programme Operations</h1><p>One governed view of programmes, projects, activities and reporting.</p></div><div className="title-actions"><span className="db-status"><span/> Database connected</span><Link className="primary-button" href="/dashboard/finance">Financial intelligence →</Link></div></div><section className="stats-grid" aria-label="Key metrics"><article className="metric-card metric-navy"><div className="metric-top"><span>PROGRAMMES</span><span className="metric-icon">▣</span></div><strong>{activeProgrammes}</strong><p>Active programmes</p><small>{programmesTotal} programme records in the system</small></article><article className="metric-card metric-blue"><div className="metric-top"><span>PROJECTS</span><span className="metric-icon">◫</span></div><strong>{activeProjects}</strong><p>Active projects</p><small>{projectsTotal} project records linked to programmes</small></article><article className="metric-card metric-gold"><div className="metric-top"><span>ACTIVITIES</span><span className="metric-icon">✓</span></div><strong>{activitiesTotal}</strong><p>Tracked activities</p><small>{inProgress} in progress · {planned} planned in the latest records</small></article><article className="metric-card metric-light"><div className="metric-top"><span>VOLUNTEERS</span><span className="metric-icon">♙</span></div><strong>{activeVolunteers}</strong><p>Active volunteers</p><small>{volunteersTotal} volunteer records · {activeAssignments} active assignments</small></article></section><section className="workflow-panel" id="workflow"><div className="section-heading"><div><p className="eyebrow dark">OPERATIONS</p><h2>Delivery flow</h2></div><Link className="section-note" href="/dashboard/workflow">Open workspace →</Link></div><div className="workflow-track">{[["01","Programme","Manage"],["02","Project","Manage"],["03","Activity","Deliver"],["04","Approval","Decide"],["05","Report","Monitor"]].map(([number,title,status],index)=><div className="workflow-step" key={title}><div className={`step-number step-${index}`}>{number}</div><div><strong>{title}</strong><span>{status}</span></div>{index<4&&<i className="step-arrow">→</i>}</div>)}</div></section><div className="dashboard-grid"><section className="panel activity-panel" id="activities"><div className="panel-heading"><div><p className="eyebrow dark">OPERATIONS</p><h2>Recent activities</h2></div><Link href="/dashboard/workflow#activities">Manage →</Link></div><div className="activity-table">{recentActivities.length===0?<div className="more">No activities have been created yet.</div>:recentActivities.map(activity=>{const tone=activity.status==="IN_PROGRESS"?"blue":activity.status==="COMPLETE"?"navy":"gold";return <article className="activity-row" key={activity.id}><div className={`activity-mark ${tone}`}>{activity.title.slice(0,1)}</div><div className="activity-main"><strong>{activity.title}</strong><span>{activity.programmeCode??"Unlinked programme"}</span></div><span className={`status ${tone}`}>{activity.status.replaceAll("_"," ")}</span><time>{formatDate(activity.dueDate)}</time></article>})}</div></section><section className="panel progress-panel" id="projects"><div className="panel-heading"><div><p className="eyebrow dark">PROJECT HEALTH</p><h2>Delivery progress</h2></div><span className="more">LIVE</span></div><div className="progress-visual"><div className="donut" style={{background:`conic-gradient(var(--school-bus-yellow) 0 ${activityHealth}%, #e9eef2 ${activityHealth}% 100%)`}}><div><strong>{activityHealth}%</strong><span>activity health</span></div></div><div className="progress-copy"><strong>Current delivery</strong><span>{activeProjects} active projects · {activitiesTotal} activities</span><div className="bar"><i style={{width:`${activityHealth}%`}}/></div><small>Health reflects the latest activity records available in the system.</small></div></div><div className="mini-metrics"><div><strong>{projectsTotal}</strong><span>Projects</span></div><div><strong>{activitiesTotal}</strong><span>Activities</span></div><div><strong>{reportsTotal}</strong><span>Reports</span></div></div></section></div><div className="dashboard-grid lower-grid"><section className="panel notice-panel"><div className="panel-heading"><div><p className="eyebrow dark">REACH & CAPACITY</p><h2>Programme reach</h2></div><Link href="/dashboard/volunteers">Volunteer capacity →</Link></div><div className="progress-copy"><strong>Beneficiary target</strong><span>0 / {BENEFICIARY_TARGET.toLocaleString("en-US")} beneficiaries recorded</span><div className="bar"><i style={{width:"0%"}}/></div><small>The 100,000 target is reserved for verified beneficiary records and will update as authoritative data is captured.</small></div><div className="mini-metrics"><div><strong>{activeVolunteers}</strong><span>Active volunteers</span></div><div><strong>{activeAssignments}</strong><span>Active assignments</span></div><div><strong>{volunteersTotal}</strong><span>Volunteer records</span></div></div></section><section className="panel quick-panel" id="reports"><div className="panel-heading"><div><p className="eyebrow dark">QUICK ACCESS</p><h2>Core actions</h2></div></div><div className="quick-grid"><Link href="/dashboard/workflow#programmes"><span>＋</span><strong>New programme</strong><small>Start a governed record</small></Link><Link href="/dashboard/workflow#projects"><span>＋</span><strong>New project</strong><small>Link to a programme</small></Link><Link href="/dashboard/workflow#activities"><span>＋</span><strong>New activity</strong><small>Track delivery</small></Link><Link href="/dashboard/workflow#reports"><span>＋</span><strong>New report</strong><small>Capture narrative</small></Link></div></section></div><footer className="dashboard-footer"><span>VSI IMS</span><span>Authoritative data workspace</span><span>© Visionary Students Initiative</span></footer></div></section></main>;
}

// Staging deployment sync: keep core dashboard available while additive volunteer migration is applied.